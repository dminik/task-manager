import { MemoryRouter } from 'react-router-dom';
import { Footer } from '../Footer/Footer';

export default {
  title: 'Footer',
};

export const Usage = () => (
  <MemoryRouter>
    <Footer />
  </MemoryRouter>
);
