import { Footer } from '../Footer/Footer';
import { MemoryRouter } from 'react-router-dom';

export default {
  title: 'Footer',
};

export const Usage = () => (
  <MemoryRouter>
    <Footer />
  </MemoryRouter>
);